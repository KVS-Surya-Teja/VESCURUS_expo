<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude the debug session dumps so raw camera frames never leak into
         Google Drive auto-backup. Applies on API <31 (see data_extraction_rules
         for API 31+). -->
    <exclude domain="external" path="debug/" />
</full-backup-content>
